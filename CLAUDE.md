# prepare-ai-review-for-kicad

KiCad の中間ネットリストから、AI レビュー用のテキストファイルを生成するツール。
本体は `erc_export.py` の単一スクリプト、テストは `tests/` にある。

## 開発の進め方

**テストファーストで進めること。** 機能追加・バグ修正のいずれでも、次の順序を守る。

1. まず期待する振る舞いを表すテストを `tests/test_erc_export.py` に書く。
2. テストを実行し、**意図した理由で失敗すること**を確認する（実装漏れによる失敗であって、
   タイポや import エラーによる失敗ではないこと）。失敗内容をユーザーに報告する。
3. その後で実装を書き、テストが通ることを確認する。

先に実装してから後追いでテストを書くのは避ける。

## テストを書く範囲

テストは無尽蔵に増やさない。**このツールの本質は「レビュー用ファイルの中身が正しいこと」**であり、
そこから遠い部分にはテストを書かない。

書く（本質的）:

- ネットリストの解析（`parse_netlist`）
- 異常検出（`detect_anomalies`）
- レビューファイルの生成（`format_review`）とその出力フォーマット
- 生成物が壊れる経路（ERC のキャンセル時にレビューが欠ける、など）

書かない（非本質）:

- 進捗ダイアログ、DPI 認識、ログの見た目など、レビューファイルの中身に影響しない部分
- Win32 API 呼び出しのような、実機でしか本当の正しさを確認できない部分

判断に迷ったときの目安:

1. **壊れたときレビュー結果が静かに間違うか？** → Yes ならテストする。
   画面を見れば一目で分かる類の不具合なら、テストの価値は低い。
2. **モックの量が検証内容を上回っていないか？** → モック比率が高いテストは、
   対象が非本質であることの兆候。「自分が用意したモックを自分の書いた順で呼ぶ」
   ことの確認はトートロジーであり、書かない。
3. **テストできる部分の価値が低く、価値の高い部分がテストできない構図になっていないか？**
   → その場合はテストを書かず、実機確認に委ねる旨をユーザーに伝える。

## テストの実行

```sh
python3 -m unittest discover -v
```

リポジトリのルートディレクトリで実行する（README.md の「テスト」節と同じコマンド）。
ルート以外から実行すると `erc_export` を import できずに失敗する。

## 出力フォーマットを変更するとき

`format_review()` の出力は AI に読ませることを前提としたテキスト。
セクションを増やしたり表記を変えたりした場合は、ファイル冒頭のヘッダにある
セクション構成の説明・凡例も併せて更新する。

### 略称にするかどうか

**原則、凡例を引かずに 1 行だけ読んで意味が取れる表記を優先する。**
トークンの節約より、読み手（AI）が凡例まで戻らずに済むことの方が効く。
ただし冗長さで文字数が膨らむのも避けたいので、次で判断する。

略称にしない:

- 1 部品に 1 回しか出ない**ラベル**（`Footprint:`, `Manufacturer:`, `Manufacturer PN:`）。
  出現回数が少ないので完全名のコストは小さい。`MFR` / `MPN` のような略称は使わない。
- 略すと情報が落ちる**値**。フットプリント名は長いが自己記述的
  （`C_0603_1608Metric_Pad1.08x0.95mm_HandSolder` にはパッド寸法まで入っている）で、
  略称化すると内容が失われるため、KiCad の値をそのまま出す。

略称にしてよい:

- 全ピン行に繰り返し現れ、取りうる値が少なく覚えやすいもの。
  ピンタイプ（`pwr` / `in` / `out` / `bi` / `tri` / `pas`）が該当し、略称のまま維持する。

迷ったら、**削減できる文字数を実際に測ってから**判断する。過去に検討した
フットプリント名の略称化は、凡例の追加分を差し引くと全体の約 3% にしかならず、
参照コストと情報欠落に見合わないため見送った。
